# Backend (Go API). Copy to .env and adjust if needed - config.Load() reads
# these two directly; everything else lives in config/*.toml.
PORT=8080
MONGO_URI=mongodb://localhost:27017

# APP_ENV selects which config/<env>.toml overlays config/default.toml.
# dev (default) | test
APP_ENV=dev
